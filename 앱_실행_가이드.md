# SafeFam 프론트(Flutter) — 다른 PC에서 실행하기

세이프팸 앱(Flutter)을 새 컴퓨터에서 빌드·실행하기 위한 안내입니다. 순서대로 따라 하면 됩니다.

> 기준: **Flutter 3.44.4 (stable)** · 대상 기기 Pixel 7 / Android 에뮬레이터 또는 실기기.

---

## 0. 사전 준비물

| 도구 | 비고 |
|---|---|
| **Flutter SDK 3.44.x** | `flutter --version`으로 확인. [flutter.dev/setup](https://flutter.dev/setup) |
| **Android Studio + Android SDK** | 에뮬레이터(AVD) 생성 또는 실기기 연결에 필요 |
| **Git** / **GitHub CLI(`gh`)** | 레포가 org(private)이라 접근 권한 필요 |
| (선택) **Docker Desktop** | 백엔드를 로컬로 띄워 로그인까지 테스트할 때만 |

설치 후 `flutter doctor` 실행 → Android toolchain·연결 기기 항목이 ✅ 인지 확인.

---

## 1. 레포 클론

SafeFam 프론트 레포는 org(private)입니다. **org에 초대된 GitHub 계정**으로 접근하세요.

```bash
gh auth login                       # 브라우저로 org 접근 권한 있는 계정 로그인
gh repo clone SafeFam/SafeFam_FE
cd SafeFam_FE
```

> ⚠️ 경로에 **공백·한글·OneDrive**가 없는 곳에 두는 걸 권장(예: `C:\Projects\SafeFam_FE`). 한글 경로면 아래 **부록 A**의 우회가 필요합니다.

---

## 2. Firebase 설정 파일 만들기 (필수 · gitignore라 레포에 없음)

FCM 때문에 앱 시작 시 Firebase 초기화가 돌아서, 이 두 파일이 **없으면 빌드/실행이 실패**합니다. 보안상 레포에 커밋되지 않으니 각자 만들어야 합니다.

1. [Firebase 콘솔](https://console.firebase.google.com) → **safefam** 프로젝트 접속(팀에 권한 요청)
2. 프로젝트 설정 → Android 앱 → `google-services.json` 다운로드 → **`android/app/`** 에 넣기
3. FlutterFire CLI로 `firebase_options.dart` 생성:
   ```bash
   dart pub global activate flutterfire_cli
   flutterfire configure --project=safefam-55bff
   ```
   → `lib/firebase_options.dart` 가 생성됐는지 확인

생성 위치 정리:
- `android/app/google-services.json`
- `lib/firebase_options.dart`

---

## 3. 카카오 앱 키

앱이 카카오 SDK를 초기화할 때 **네이티브 앱 키**를 `--dart-define`으로 받습니다(값은 이미 레포 `android/app/src/main/AndroidManifest.xml`의 `com.kakao.sdk.AppKey` / 리다이렉트 스킴에 들어 있으니 거기서 확인하거나 팀 리드에게 요청).

실행할 때 아래처럼 넘깁니다(다음 단계 명령에 포함):
```
--dart-define=KAKAO_NATIVE_APP_KEY=<AndroidManifest의 앱키>
```
> 카카오 네이티브 키는 APK에 실려 배포되는 반공개값이라 그 자체는 비밀이 아니지만, 이 문서엔 값 대신 참조만 적습니다. 카카오 로그인이 실제로 되려면 **카카오 콘솔에 이 PC/기기의 키 해시가 등록**돼 있어야 합니다(안 되면 카카오 로그인만 실패, 나머지는 정상).

---

## 4. 의존성 설치 & 실행

```bash
flutter pub get
```

### (A) 에뮬레이터에서 실행 — 가장 간단
안드로이드 에뮬레이터를 켠 뒤:
```bash
flutter run --dart-define=KAKAO_NATIVE_APP_KEY=<앱키>
```
- 백엔드 주소 기본값은 **`http://10.0.2.2:8080`**(에뮬레이터가 보는 호스트 PC의 localhost). 로컬 백엔드를 같이 띄우면(5번) 바로 붙습니다.
- 백엔드 없이 UI만 볼 거면 그대로 실행 — 로그인·분석만 실패하고 화면 탐색은 됩니다.

### (B) 실기기(USB)에서 실행
1. 휴대폰: 설정 → 휴대전화 정보 → **빌드번호 7번 탭** → 개발자 옵션 → **USB 디버깅 ON**
2. USB 연결 → 폰의 "USB 디버깅 허용" 창 허용 → `flutter devices`에 폰이 뜨는지 확인
3. 실행:
   ```bash
   flutter run -d <폰ID> --dart-define=KAKAO_NATIVE_APP_KEY=<앱키>
   ```
   > ⚠️ **실기기는 `10.0.2.2`로 백엔드에 못 붙습니다.** 로컬 백엔드를 쓰려면 같은 Wi-Fi에서 PC의 LAN IP로:
   > `--dart-define=SAFEFAM_API_BASE_URL=http://<PC의_LAN_IP>:8080`
   > + 디버그 평문 허용 설정(`android/app/src/debug/res/xml/network_security_config.xml`)에 그 IP를 추가, + PC 방화벽 8080 인바운드 허용, + 학교/회사 Wi-Fi는 기기 격리로 막힐 수 있음. (백엔드 안 붙이면 무시)

### (C) APK 파일로 배포
```bash
flutter build apk --debug --dart-define=KAKAO_NATIVE_APP_KEY=<앱키>
```
→ 결과물 `build/app/outputs/flutter-apk/app-debug.apk` 를 폰으로 옮겨 설치("출처를 알 수 없는 앱" 허용). debug APK라 서명·구글서비스 그대로 동작합니다.

---

## 5. (선택) 백엔드를 로컬로 띄워 로그인까지 테스트

백엔드 레포(`SafeFam_BE`)에 docker-compose가 있어 Docker만 있으면 됩니다. **로그인 테스트엔 spring만** 올리면 됩니다(postgres·rabbitmq는 의존성으로 자동 기동).

```bash
gh repo clone SafeFam/SafeFam_BE
cd SafeFam_BE
```

1. `.env` 생성(필수 시크릿 — 기본값 없음):
   ```bash
   # JWT 서명키 (CRLF 없이)
   printf 'JWT_SECRET=%s\n' "$(openssl rand -base64 64 | tr -d '\r\n')"  >  .env
   # Outbox 암호화키 — 반드시 32바이트(base64 44자)
   printf 'OUTBOX_ENCRYPTION_KEY_BASE64=%s\n' "$(openssl rand -base64 32 | tr -d '\r\n')" >> .env
   ```
2. 기동:
   ```bash
   docker compose up -d --build spring-backend
   ```
   → `http://localhost:8080/actuator/health` 가 UP 이면 준비 완료(스키마는 Flyway가 자동 생성).
3. 테스트 유저 시드(가입은 실제 SMS 발송이 필요해 DB에 직접 넣음):
   ```bash
   docker exec -i safefam-postgres psql -U postgres -d safefam <<'SQL'
   CREATE EXTENSION IF NOT EXISTS pgcrypto;
   INSERT INTO users (phone_number, email, name, role, password_hash, is_onboarded)
   VALUES ('01011112222','test@safefam.local','테스트유저','USER',
           crypt('Test1234', gen_salt('bf',10)), TRUE)
   ON CONFLICT (phone_number) DO UPDATE
     SET password_hash = EXCLUDED.password_hash, is_locked = FALSE, login_fail_count = 0;
   SQL
   ```
   → 앱에서 **휴대폰 `01011112222` / 비밀번호 `Test1234`** 로 로그인.
4. 종료: `docker compose down` (데이터까지 지우려면 `-v`)

---

## 부록 A. Windows 사용자 이름에 한글이 있을 때 (예: `C:\Users\홍길동\...`)

이 경우에만 해당합니다. 사용자 이름이 영문이면 **건너뛰세요.**

`jni` 네이티브 플러그인의 CMake/ninja 빌드가 한글 경로를 못 읽어 빌드가 실패합니다
(`ninja: error: FindFirstFileExA(...): 구문이 잘못되었습니다`). ninja가 ASCII 경로만 처리하므로, pub 캐시를 ASCII 이름의 **정션**으로 우회합니다(정션은 관리자/개발자 모드 불필요).

```powershell
# 1) pub 캐시에 ASCII 정션 생성
cmd /c mklink /J C:\pubjunction "$env:LOCALAPPDATA\Pub\Cache"
# 2) 스테일 네이티브 빌드 캐시 제거
Remove-Item -Recurse -Force C:\pubjunction\hosted\pub.dev\jni-1.0.0\android\.cxx -EA SilentlyContinue
# 3) PUB_CACHE를 정션으로 지정(현재 세션 + 영구)
$env:PUB_CACHE='C:\pubjunction'
[Environment]::SetEnvironmentVariable('PUB_CACHE','C:\pubjunction','User')
# 4) 의존성 재해결(package_config가 ASCII 경로로 바뀜)
flutter pub get     # 끝에 "Developer Mode/symlink" 경고로 종료돼도 파일은 정상 기록됨 — 무시
# 5) pub 재resolve(심링크 게이트)를 건너뛰고 실행
flutter run --no-pub -d <기기> --dart-define=KAKAO_NATIVE_APP_KEY=<앱키>
```
> 원리: 문제는 경로 **문자열**의 한글이므로, 저장소는 그대로 두고 순수 ASCII 이름(`C:\pubjunction`)으로 접근하면 해결됩니다.

---

## 자주 겪는 문제

| 증상 | 원인 / 해결 |
|---|---|
| 실행 시 Firebase 관련 크래시 | 2번 파일(`google-services.json`·`firebase_options.dart`) 누락 |
| `ninja: FindFirstFileExA ... 구문이 잘못` | 한글 사용자 경로 → **부록 A** |
| `Building with plugins requires symlink support` | Windows 개발자 모드 꺼짐. 부록 A의 `--no-pub` 경로로 우회하거나 개발자 모드 활성화 |
| 로그인/분석이 "네트워크 오류" | 백엔드 미기동(5번) 또는 실기기가 `10.0.2.2` 사용(4-B의 LAN IP 설정) |
| 카카오 로그인만 실패 | 카카오 콘솔에 그 기기의 키 해시 미등록(나머지 기능엔 영향 없음) |

---

문의는 프론트 담당(이건)에게. 백엔드 계약·환경 상세는 레포 `README.md` 참고.
